<!DOCTYPE html>
<html lang="en">
<head>
    <title>User Site</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/style.css" rel="stylesheet">
</head>
<body>

<header class=text-center>
    <h2>User Site</h2>
    <p>Login, Join, Show, Edit, Etc.</p>
</header>
<new-row>
<col-3></col-3>

<col-6>
    <article>
    <header><strong>Edit <%=user.fullName%></strong></header>
    <form action="/site/users/<%=user._id%>/edit" method=post>
    <form-horizontal>
        <section>
        <div>
            <form-label-3>Email</form-label-3>
            <form-input-8><input type=text name=email value="<%=user.email%>"></form-input-8>
        </div>
        
        <div>
            <form-label-3>First Name</form-label-3>
            <form-input-8><input type=text name=first minlength=3 value="<%=user.first%>"></form-input-8>
        </div>
        
        <div>
            <form-label-3>Last Name</form-label-3>
            <form-input-8><input type=text name=last minlength=3 value="<%=user.last%>"></form-input-8>
        </div>
        
        <div>
            <form-label-3>Birthday</form-label-3>
            <form-input-8><input type=date name=bday value="<%=user.bday%>"></form-input-8>
        </div>
        <!-- <div>
            <form-label-3>Gender</form-label-3>
            <form-input-8>
                <select name=gender>
                    <option value=Male>Male</option>
                    <option value=Female>Female</option>
                </select>
            </form-input-8>
        </div> -->
        </section>
    <footer class=text-center>
        <input type=submit value='Submit Changes'>
        <a href="/site/users/<%=user._id%>"><button-link>Go to User Page</button-link></a>
    </footer>
    </form-horizontal>
    </form>
    </article>
</col-6>

<col-3>
    <% if(flashes.length !== 0) { %>
    <alert-danger>
        <strong>Please fix the following:</strong><br>
        <% for(let flash of flashes.reverse()){ %>
            - <%=flash%><br>
        <% } %>
    </alert-danger>
    <% } %>
</col-3>

</new-row>
</body>
</html>